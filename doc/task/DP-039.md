# DP-039: Publish Fast Coverage Artifacts In CI

Language: [English](#english) | [zh-TW](#zh-tw)

Source: `Detail_Plan.md` and `Plan.md`

## English

### Vibe Coding Prompt

You are working in the WOMS repository. Implement exactly one atomic coverage task: **DP-039: Publish Fast Coverage Artifacts In CI**.

Use the project direction from `Plan.md`: improve coverage for deployment-critical WOMS behavior while keeping PostgreSQL and Redis integration tests gated/manual, preserving deterministic scheduling behavior, and keeping command packages in coverage scope.

### Repository Rules

- Keep the change atomic: implement only this DP item.
- Do not use git commands for this task.
- Use TDD or add the requested verification before/with the behavior change.
- Preserve UTF-8 for all source, SQL, Markdown, and frontend files.
- Do not introduce secrets, `.env`, local volumes, build output, caches, or private IDE settings.
- If implementation or deployment behavior changes, update `README.md`, `README.en.md`, and `README.zh-TW.md` in the same change.
- Run the verification command listed in this prompt; run `npm run test:coverage` as broader validation when practical.

### Task Details

Target files:

- `.github/workflows/ci.yml`

Modification:

- Upload `coverage.out` and web coverage output from the existing fast CI jobs.

Verification:

```sh
npm run test:coverage
```

Acceptance criteria:

- Existing fast validation behavior remains fast.
- Artifact upload does not change coverage thresholds.

### Required Workflow

1. Read the target files and nearby tests before editing.
2. Add or adjust the smallest implementation needed for this DP.
3. Add focused tests or verification exactly within this DP boundary.
4. Run the verification command from the DP.
5. Update documentation only when the DP changes commands, behavior, or local setup.
6. Summarize changed files, verification results, and any residual risk.

## zh-TW

### Vibe Coding Prompt / 氛圍 Coding 提示

你正在 WOMS repository 中工作。請只實作一個原子化覆蓋率任務：**DP-039: Publish Fast Coverage Artifacts In CI**。

請依 `Plan.md` 的方向：提升部署關鍵行為的測試覆蓋率；PostgreSQL 與 Redis integration tests 維持 gated/manual；排程行為必須 deterministic；command packages 必須保留在 coverage 範圍內。

### 專案規則摘要

- 維持原子化：只實作此 DP 項目。
- 此任務不使用 git commands。
- 採 TDD，或至少在行為變更前後加入指定測試或驗證。
- 所有 source、SQL、Markdown、前端檔案都必須維持 UTF-8。
- 不得加入 secrets、`.env`、本機 volume、build output、cache、IDE 私有設定。
- 若實作或部署行為改變，需在同一次變更更新 `README.md`、`README.en.md`、`README.zh-TW.md`。
- 必須執行本提示列出的驗證命令；可行時執行 `npm run test:coverage` 作為較完整驗證。

### 任務細節

請完整對齊英文區塊的 Task Details：目標檔案、修改內容、驗證命令與驗收標準都以同一個 DP 的範圍為準，不要擴張到其他 DP。

### 必要流程

1. 編輯前先閱讀目標檔案與鄰近測試。
2. 只加入此 DP 所需的最小實作變更。
3. 在此 DP 邊界內加入聚焦測試或驗證。
4. 執行 DP 指定的驗證命令。
5. 只有當命令、行為或本機設定改變時，才同步更新文件。
6. 最後摘要變更檔案、驗證結果與剩餘風險。

### Implementation Progress

- Status: Verified
- Agent/session: Codex DP-037-through-DP-044 slice
- Started: 2026-05-28 21:43 CST
- Finished: 2026-05-28 21:50 CST
- Changed files:
  - `.github/workflows/ci.yml`
  - `doc/task/DP-039.md`
- Verification:
  - `npm run test:coverage`: Passed
- Notes:
  - Fast CI now uploads Go `coverage.out` and web `coverage/**` artifacts without changing the fast job set.
