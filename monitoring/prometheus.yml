global:
  scrape_interval: 1s
  evaluation_interval: 5s

scrape_configs:
  # ── Go API service metrics ──────────────────────────────────────
  - job_name: woms-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:8080
        labels:
          service: woms-api

  - job_name: woms-web-nginx
    metrics_path: /metrics
    static_configs:
      - targets:
          - nginx-exporter:9113
        labels:
          namespace: woms
          pod: compose-web-1
          service: woms-web

  # ── Gthulhu infrastructure metrics (Postgres/Redis/Kafka) ──────
  # - job_name: gthulhu
  #   metrics_path: /metrics
  #   static_configs:
  #     - targets:
  #         - gthulhu:9091
  #       labels:
  #         service: gthulhu
